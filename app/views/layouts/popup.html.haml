= render 'layouts/header'

%body
  #layout
    #logo
      %a{href: root_url}
    #container
      #content
        #content_main
          %h1= @cms_page.title
          = blog @cms_page.body
          = yield

    #footer_spacer
  = render 'layouts/footer'