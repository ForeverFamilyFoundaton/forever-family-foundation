<%= simple_form_for(@relationship) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :typeof %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
